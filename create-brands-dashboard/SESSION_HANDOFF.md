# CHOCOBERRY DASHBOARD — SESSION HANDOFF
**Updated: 2026-06-07 v14 Sun 07-06 eve (COGS DAY TWO: architecture SIGNED OFF, data pack issued, channel economics, modifier discovery) (ask-data slice 3 v1.1 LIVE + next-session queue specced: chat v2 agent, anomaly explainer, Google reviews). Supersedes c9c5741 version.**
**Marker for deploy verification: HANDOFF_V14**

---

## 1. PROJECT & PEOPLE
- **Owner**: Atif Razzaq (atifrazzaqfast@gmail.com) — owner-operator of Chocoberry (~20-store UK hospitality chain: owned / franchise / JV) + second smaller brand **Tove** (~£13.5k/wk).
- **App**: "Chocoberry Dashboard" / create-brands-dashboard — React (CRA) ops & staff-management dashboard.
- **Stack**: React + Tailwind (core utility classes only), Supabase (PRODUCTION project id `qtjsdbasoouslcpinqhu`, **PRO PLAN since 06-06** — 8GB db, daily backups kept 7 days, no inactivity pausing; upgraded after free-tier 115% db-size limit breach), Vercel hosting.
- **Prod URL**: create-brands-dashboard.vercel.app · **GitHub**: github.com/CreateBrands/create-brands-dashboard
- **Local repo**: `C:\dev\create-brands-dashboard\create-brands-dashboard` (the DOUBLED folder name is real). Windows CMD user — no `\` line continuations, one command per line.

## 2. CODEBASE & CONVENTIONS
- Two giant files: `src/App.js` (~1.25 MB) and `src/supabase.js` (~186 KB). All components live in App.js (function declarations, hoisted).
- **App.js imports BARE hooks** (`import { useState, useMemo, useEffect, useCallback, useRef } from "react"`). **NEVER write `React.useMemo` etc.** — broke a prod build once. Check `grep React\.use` = 0 before deploy.
- Roles: owner / hq_staff / manager / staff / employee / trainee. Helpers `isOwnerRole`, `isHqOrAbove`. **RLS is OFF everywhere** — all access gating is app-side.
- ~76 ops_team members across stores; brand ids `chocoberry`, `tove`.
- `useExcelJS()` hook (~line 585) lazy-loads ExcelJS from CDN for styled Excel exports.
- recharts + lucide-react imported. Manager login screen (~line 2573) compares PLAIN-TEXT passwords (known debt; dormant Supabase Auth fns exist in supabase.js for future real passwords).
- Validation before every deploy (assistant runs in its sandbox): babel parse check, eslint hooks config, eslint no-undef config (React removed from globals deliberately).

## 3. DEPLOY DISCIPLINE (hard-won — follow exactly)
1. Assistant produces full files in outputs with **distinct filenames per iteration** + exact byte counts.
2. User downloads → **`dir "C:\Users\conta\Downloads\<name>*.js"` FIRST** — newest file is often `"(1)"`-suffixed; the plain name is usually STALE. When in doubt: `del` old ones, re-download.
3. `copy /Y` newest → `src\`.
4. **Verify gate**: `findstr` for a marker string unique to the new version + `for %F in (file) do @echo %~zF bytes` exact byte match. **Empty findstr = STOP, do not commit.** (Stale-file commits happened 3× in one night.)
5. Use **`&&`-gated chain** so verification physically blocks git: `findstr /c:"MARKER" src\App.js && git add ... && git commit -m "..." && git push`.
6. Supabase SQL editor: run statements ONE AT A TIME; never paste query OUTPUT back into the editor (caused repeated syntax errors).
7. Edge Function updates: **edit IN-PLACE in the dashboard editor of the existing function** — uploading a file once created an accidental NEW function that nothing calls (since deleted).

## 4. FLIPDISH SALES PIPELINE (the heart of the data platform)
- **Live source**: `flipdish_sales` table, fed by Edge Function **`flipdish-rms-sync`** (pulls per-sale data from RMS Reporting API, franchisor id 678cacc0-..., property→store matched by name candidates from `deriveShortNameCandidates`; store lookup is `.in("brand_id",["chocoberry","tove"])`).
- **DEAD**: `flipdish_orders`, `flipdish_sync_log` (old /orders webhook pipeline, stopped 19 May). flipdishOrders=[] in app → **no customer-level data exists** (no names/tips/fees) → customer-level AI impossible.
- **Function behaviour**: empty `{}` body = **ROLLING WINDOW yesterday+today** (gap days self-heal). Explicit `{"fromDate","toDate"}` = backfill; processes **MAX 2 days/invocation** (~40s), returns `resumeFrom` for longer ranges. Upsert conflict key **(sale_id, brand_id)** — NB: if a sale's brand classification changes, re-sync INSERTS A DUPLICATE under the new brand (bit us on 19 May Tove data).
- **Auth**: Verify-JWT is OFF (legacy-JWT gate caused the 1–14 June silent outage: cron's hardcoded legacy service-role JWT stopped validating → 401 UNAUTHORIZED_INVALID_JWT_FORMAT). Protection = **shared-secret guard**: env `SYNC_SHARED_SECRET` on the function; callers must send header `x-sync-secret`. ⚠️ Current secret value was EXPOSED in chats/docs — **rotation is open item 2**; must match in 3 places: function env, cron job 5 header, Vercel `REACT_APP_SYNC_SECRET` (+ redeploy, CRA bakes at build).
- **Tokens**: TWO portal session cookies read per-request from `sync_config` table (keys `flipdish_rms_token` ~24h TTL, `flipdish_fd_authorization` ~Sep 2026). Kept fresh by Chrome extension **"Chocoberry — Flipdish Token Refresh"** (4-h alarm; ONLY works while Chrome open AND Flipdish portal logged in; portal logout → "Failed to fetch" → token rot → sync 401s from Flipdish within ~24h). STRUCTURAL FRAGILITY — recurs; long-term fix = always-on host or official API creds (Phase 0.4).
- **Cron jobs** (pg_cron): job 1 punch-photo-cleanup 03:00 · **job 5 `flipdish-daily-sync`** `0 6,12,18 * * *` → net.http_post to the function with x-sync-secret · **job 6 `flipdish-health-check`** `30 6,12,18 * * *` → `check_flipdish_sync_health()` · **job 7 `store-day-aggregates-refresh`** `0 7 * * *` → `refresh_store_day_aggregates(3)` · **job 9 `labour-day-aggregates-refresh`** `5 7 * * *` → `refresh_labour_day_aggregates(14)` · **job 10 `store-day-forecasts-generate`** `15 7 * * *` → `generate_store_day_forecasts_v2(7)` · **job 12 `forecast-anomaly-check`** `25 7 * * *` → `check_forecast_anomalies()` · **job 13 `weekly-narrative`** `40 7 * * 1` (Mondays) → `weekly-narrative` fn with x-sync-secret · **job 14 `store-day-payments-refresh`** `50 6 * * *` → `refresh_store_day_payments(3)` · **job 15 `item-day-aggregates-refresh`** `52 6 * * *` → `refresh_item_day_aggregates(3)` · **`weather-sync-daily`** `45 6 * * *` → `weather-sync` fn (no secret). MORNING SEQUENCE: 06:00 sales → 06:45 weather → 07:00 sales agg → 07:05 labour agg → 07:15 forecasts → 07:25 anomaly check → 07:40 Mon-only narrative.
- **net.http_post is fire-and-forget**: real result in `net._http_response` by returned request id (`SELECT * FROM net._http_response WHERE id=<req_id>` after ~40-60s).
- **Diagnostic playbook**: freshness query `SELECT business_date, count(*), max(sale_time), count(*) FILTER (WHERE sale_time::time > '18:00') FROM flipdish_sales WHERE business_date >= current_date-7 GROUP BY 1 ORDER BY 1 DESC;` — healthy days end ~02:40 next morning with 1000-2000 evening sales; **partial-day fingerprint = sales>0 but ZERO after 18:00**; all-stores-down-in-lockstep on dashboard = partial sync, NOT trading collapse.
- **Manual trigger**: net.http_post with the secret header; manual backfill body `{"fromDate":"YYYY-MM-DD","toDate":"YYYY-MM-DD"}`.
- **Sync-health watchdog** (`check_flipdish_sync_health`, sync_health_check.sql, RUN): STALE >20h → "Flipdish sync may be down"; PARTIAL yesterday → "Yesterday's sales look incomplete" + backfill instruction. Inserts kind='sync' notifications to owner/hq bells. **DEDUPE: no new alert while an UNREAD same-title sync alert exists → user must DISMISS handled alerts or future ones are muffled.** Manual tests before 06:30 UTC false-positive on yesterday (evening arrives via 06:00 run) — field-proven day 1.

## 5. DATA PLATFORM — PHASE 0.2 (DONE)
- **`store_day_aggregates`** table: PK (brand_id, store_id, business_date); orders, revenue_gross/net, tax, discounts, atv, cancelled_count, refunded_count, **by_channel jsonb**, **by_hour jsonb (Europe/London hours — for hourly forecasting)**, first/last_sale. Unmatched sales aggregate under store_id **'unmatched'** (data-quality alarm, currently 0 rows everywhere).
- **`refresh_store_day_aggregates(days_back int)`**: **REBUILD semantics** (DELETE date range, then INSERT) — original upsert-only version left stale rows when a grouping key disappeared (caused double counting). Canonical file `store_day_aggregates.sql` carries the fixed version. Nightly cron job 7 runs `(3)`; manual backfill `(60)`.
- **Reconciled to the penny** vs raw sales. ~587 rows: 21 stores × ~28 trading days (clean data from ~19 May).
- **Tove saga (resolved)**: Tove revenue (£13.5k/wk) was invisible in 'unmatched'. Fixes: existing store record `store-tove` used; all tove sales `UPDATE ... SET store_id='store-tove'`; Edge Function store lookup extended to both brands (in-place edit, verified `tove_unmatched=0` after a real sync); **19 May day-one sales were mis-branded `chocoberry` with property "Tove"** → re-sync created correct `tove` copies as DUPLICATES (different conflict key) → deleted the mis-branded originals (`DELETE FROM flipdish_sales WHERE brand_id='chocoberry' AND property_name='Tove'`). NB: 19 May chocoberry chain total dropped ~£2k = correction.
- Daily revenue shape (sanity reference): weekdays ~£29-33k, weekends ~£55-65k chain-wide; Tove £1-3.3k/day.

## 5B. FORECAST ENGINE — PHASE 1 v1+v2 (DONE 06-06)
**Files**: phase1_forecast_engine_v1_20260606.sql · phase1_hourly_v1_20260606.sql · forecast_v2_20260606.sql · weather-sync-edge-function_20260606.ts — ALL RUN/DEPLOYED.
- **`store_day_forecasts`** PK (brand_id, store_id, business_date, **horizon_days**) — forecasts kept PER HORIZON so accuracy reports per lead time. Cols: forecast_revenue/orders, basis_points (history points used, max 4 — <3 = "thin", self-labelled), method, **factors jsonb** (v2: base_revenue, event_factor, event_name, weather_factor — drives the "why this number" UI).
- **Model v2 (`generate_store_day_forecasts_v2(7)`, LIVE on cron job 10)**: BASE (weighted avg last 4 same-weekdays, weights 4/3/2/1, **excluding calendar-event days from the basis**) × EVENT factor × WEATHER factor. v1 (`generate_store_day_forecasts`) retained; `forecast_accuracy_by_method` view will answer whether factors beat plain v1.
- **`calendar_events`**: bank holidays, religious (Ramadan/Eids through 2027 — Islamic dates APPROX, confirm by moon sighting), school holidays (council-approximate), local events; chain/brand/store scoped; prior_uplift editable multipliers; exclude_from_basis flag. **Store closures = store-scoped row with uplift 0.0** (Walsall closed: row to 2099 — DELETE it if Walsall reopens). Retroactive rows seeded for 25 May bank hol + Eid al-Adha 26-27 May + May half-term 23-31 May, which were contaminating bases.
- **Weather**: `weather_daily` (store_id+date PK; t_max/min, precip, WMO code, is_forecast) fed by Edge Function **`weather-sync`** (Verify JWT OFF, no secret, Open-Meteo keyless API) using **lat/lng columns added to stores** (all 24 geocoded, city-level). Past rows auto-flip to actuals = training data for learning real weather coefficients later. Current priors: t_max≥24 → ×1.08, precip≥8mm → ×0.95, t_max≤4 → ×0.97, else 1.0.
- **Hourly**: `store_weekday_hour_profile` (revenue share per store/dow/hour, 8-wk sum-over-sum) + `store_hour_forecasts` (daily forecast × share = intraday curve; Phase 2 staffing input). by_hour jsonb shape: `{"21":{"orders":16,"revenue":265.65},...}` Europe/London hours.
- **Accuracy**: `forecast_accuracy` (per store/day/horizon vs actuals) + `forecast_accuracy_summary` (MAPE per horizon; UI badge flips from "warming up" at ≥14 scored days). First scored day ~08 June.
- **BACKTEST 05-Jun (chain ~13% over-forecast, store-level ~19% mean abs err on 2-3 basis points)**: uniform over-forecast traced to **May half-term Friday in every basis** → fixed via retroactive calendar row. Best stores 1.5-9% err. Whitechapel +42% under (growing store, self-corrects). THE LOOP: error analysis → calendar enrichment → regenerate. Expect ~8-12% store-level err after a year of this.
- Forecast sanity reference (post-fix, w/c 07 Jun): Sun £57.2k · Mon £30.1k · Tue £30.2k · Wed £29.2k · Thu £30.6k · Fri £47.5k · Sat £58.8k.

## 5C. ANOMALY ALERTS — PHASE 2A (DONE 06-06 eve)
**File**: phase2a_anomaly_alerts_20260606.sql (+ manager-extension CREATE OR REPLACE run after).
- **`forecast_anomaly_check` view** = dry-run lens + threshold tuner: per store/scored-day verdict OK | UNDER | OVER | NO_SALES. Alert criteria: deviation BOTH >25% AND >£300 (under OR over) vs the freshest (lowest-horizon) forecast; NO_SALES = forecast >£0 but no aggregates row.
- **`check_forecast_anomalies()`** (cron job 12, 07:25): inserts kind='anomaly' notifications. RECIPIENTS mirror notifyManagers exactly: owners/hq always (link_view 'chain'); **managers in the users table** (role='manager', `c.brand_id = ANY(u.brand_ids) AND c.store_id = ANY(u.store_ids)`, link_view 'store-analytics' since managers can't see chain). NOTE: managers live in USERS (arrays), NOT ops_team — discovered by reading deployed notifyManagers.
- Dedupe = watchdog pattern: no repeat while an UNREAD same-title alert exists → DISMISS handled alerts. OVER alert bodies prompt the calendar loop ("add it to calendar_events so forecasts learn"); NO_SALES bodies distinguish closure (add calendar closure row) vs sync gap (backfill).
- **First possible firing: 08 Jun ~07:25** (scoring Sun 07 Jun, the first forecast day). kind 'anomaly' icon ⚡ shipped in app (unknown kinds fall back to 🔔 anyway).
- LEFT TO TUNE (data-gated): thresholds (watch forecast_anomaly_check for chattiness); learned coefficients replacing seeded priors.

## 5D. CLAUDE API NARRATIVE — PHASE 3 SLICE 1 (LIVE 06-06 close)
**Files**: phase3_narrative_20260606.sql + weekly-narrative-edge-function_20260606.ts.
- **Anthropic API account is SEPARATE from the Max chat subscription** (key in Edge Function secrets as `ANTHROPIC_API_KEY`, server-side only, never in app/Vercel/chat). Cost ~pennies/week (Sonnet, 1 call/wk).
- **Edge Function `weekly-narrative`** (Verify JWT OFF; gated by x-sync-secret like the sales sync — every invocation costs API money). Mondays 07:40 (cron job 13) or manual; optional body {"weekEnd":"YYYY-MM-DD"} for testing. Gathers AGGREGATES ONLY (per-store weekly revenue vs prior wk, daily chain totals, brand split, forecast accuracy, anomaly verdicts — never raw rows / customer / employee data), calls claude-sonnet-4-6 with a NARRATE-ONLY prompt (never invent or recompute numbers), stores in **`narrative_reports`** (body + input_data jsonb audit trail of exactly what was sent), bells owner/hq with kind='report' (falls back 🔔) + first paragraph.
- **First report (wk 30 May–5 Jun) verified**: every figure matched knowns; it independently corroborated the backtest (Whitechapel +65% WoW = the store that beat forecast +42%). KNOWN GAP for v2: the bundle lacks calendar_events context, so the −22% WoW headline vs the bank-hol/Eid/half-term-inflated prior week read as a demand dip — ADD overlapping calendar_events to the bundle + prompt line "note when either week contained events that distort the comparison".
- ⚠️ During setup the env secret was found saved under the NAME `NEW_SECRET` (placeholder became the name during the afternoon swap) → fixed: deleted, re-added as `SYNC_SHARED_SECRET` (32 chars). Implication: between the swap and this fix the sales sync's secret gate was checking against a missing env. DEBUG PATTERN THAT WORKED: temporary diag block returning env/header LENGTHS only (never values) — header_len 20 exposed that the SQL placeholder text `PASTE_32_CHAR_SECRET` was being sent literally.
- **NARRATIVE v2 LIVE (file weekly-narrative-V2_20260606.ts, deployed in-place)**: bundle now includes calendar_events overlapping EITHER week (with in_this_week/in_prior_week flags) + prompt rule "never present an event-driven swing as organic demand change"; link_view → 'weekly-reports'. **VERIFIED by A/B on the same week**: v1 called the −22% WoW a "structural demand dip"; v2 correctly named the bank-hol+Eid-inflated baseline, reasoned half-term as WoW-neutral (it spans both weeks), softened Tove's −37% with Eid context while keeping the question open, used the Walsall closure row to advise baseline suppression, and surfaced a NEW insight (Evington Rd: chain-highest 1,684 orders on £27.5k = low ATV → menu-mix check). KNOWN WOBBLE: invented "schools back from Mon 8 June" (actually ~1-2 June) — prompt restricts inventing NUMBERS but not calendar claims; **v2.1 = add "only reference calendar facts present in calendar_events"** (parked).
- **Weekly Reports viewer LIVE in app**: nav 'weekly-reports' (next to Chain Performance, owner/hq) — report list (newest first, 26 max) + markdown-rendered body via compact MdBlock renderer (headings/bold/hr/bullets/pipe tables/paragraphs — handles the narrative's controlled format only) + provenance footer. supabase.js: fetchNarrativeReports. Notifications now deep-link here.
- TIP: save the manual-run http_post as a Supabase SQL snippet ("narrative — manual run", secret embedded) — one-click reruns.
- **SLICE 3 v1 SHIPPED (07-06 ~01:00): ask-data Edge Function + "Ask the Data" view** (nav 'ask-data', owner/hq, MessageSquare icon). v1 scope: SINGLE question → answer, NO conversation memory. Bundle (aggregates only, never raw/people data): 8wk per-store WEEKLY revenue/orders + 28d chain daily + 28d chain-level top-30 items + 7d settlement lines + next-7d forecasts + ±6wk calendar + accuracy summary. Prompt forces honesty about granularity limits ("the data can't answer that"). App calls via supabase.functions.invoke with x-sync-secret from REACT_APP_SYNC_SECRET (mirrors runFlipdishSync). **LESSON: browser-called functions NEED CORS + OPTIONS handling** (v1 failed with "Failed to send a request" — cron-called fns like weekly-narrative never hit preflight; v1.1 = ask-data-edge-function_v1_1_20260607.ts adds corsHeaders + OPTIONS, mirrors sales-sync pattern). DEPLOYED + working; user verdict: "very basic" → v2 specced below.
- **NEXT-SESSION QUEUE (user-agreed order)**:
  ① **Chat v2 — tool-use agent**: replace fixed bundle with whitelisted data-fetcher tools (sales/items/payments/forecasts/labour/schedules/onboarding/hiring/certs/calendar) + investigation loop in the Edge Fn + conversation memory in the view + server-side role scoping. **OPEN DECISION (Atif, undecided)**: employee/HR data to the Claude API? A = keep sales-only line (people questions → "use the Board/Reports"); B = cross deliberately for owner/hq (GDPR processing decision; consider pseudonymising names in transit). User's stated goal: "the chat should be able to give response to everything in the context of the app".
  ② **Slice 2 anomaly explainer**: build Monday evening AGAINST the first real anomaly (07:25 Mon), not blind.
  ④ **COGS / inventory (Nory-replacement) — DEEP ANALYSIS DONE (07-06 ~02:30), substantially de-risked**: Atif uploaded the Nory onboarding workbook (Chocoberry_-_IM_Import_Sheet_.xlsx) + 2 kitchen recipe workbooks (Baking_Unit_Rep.xlsx: 22 baking preps; Breakfast___Dinner_Recpie.xlsx: 12). ASSETS FOUND: 477-item ingredient catalogue (318 with real prices, 86 priced £0, 73 unpriced; 405 categorised; "Included in GP" flags set); 70 named suppliers (but 0 item↔supplier links); 35 structured batch recipes (avg 6.3 ingredients, ~32/34 kitchen recipes transcribed) — **BUT only 7/35 have yields (no yield = no cost/portion)**; 36 menu recipes mapped — café menu only (coffees/mojitos/faloodas). **CRITICAL FINDING: 9 of the top 13 revenue items have NO recipe mapping** (Waffle Bites #1 ~£16.8k/wk, Kanafeh Strawberry Cup, Crunch Cake, Banana Pudding, ChocBar Shake, Cookie Dough, Belgian Choc Kanafeh, Premium Shake, Dream Cakes) — coverage inversely correlated with revenue. POS-mapping sheets EMPTY in Nory's workbook but ALREADY SOLVED by item_day_aggregates (our hardest-input advantage). BUILD PLAN: (1) import workbook → ingredients/suppliers/batch_recipes/menu_recipes tables (parse from these exact files, ~evening incl. import script); (2) ATIF DATA DEBT (no code): yields for 28 batch recipes, prices for 159 items, recipes for the top-seller desserts (~15-25 items, few focused kitchen hours) — Claude to generate a bounded gap-worklist spreadsheet ranked by revenue; (3) theoretical COGS + GP%/item/store/day auto vs sales = Nory's recipe-costing pillar; (4) purchases via invoice capture (Claude-vision route on the API account); (5) weekly stock-count screen → actual COGS → variance report = full Nory loop (COGS = opening + purchases − closing; theoretical-vs-actual variance is the operator metric). Source files re-uploadable by Atif at build time.
  ③ **Google reviews daily sync — ROUTE 1, APPLICATION SUBMITTED (07-06 ~01:50)**: Cloud project `chocoberry-reviews` (number **135857250031**); APIs enabled: My Business Account Management + Business Information (+ Lodging by mistake, inert); GBP access form submitted as business owner, ~21 verified locations, anchored on 207 Evington Road — **TWO support cases opened: 1-2814000041396 (first submission, likely carried project NAME not number) and 2-3513000040917 (resubmission with correct project number 135857250031 — treat as authoritative). Review ~7-10 working days each (expect ~mid-June); if Google replies to/rejects the first, that's expected — the second is the real application**. ON APPROVAL: OAuth client + one-time consent → refresh token into fn secrets → build google_reviews table + daily reviews-sync fn ~06:45 + ≤2★ notifications + ratings in dashboard + review context into Monday narrative. NOTE: Google's docs banner mentions a newer dedicated **Reviews API** — prefer it over legacy mybusiness v4 at build time. Store mapping: Google listing names/addresses → stores table (same matching exercise as the Flipdish sync; some quirky listing names like "Asda St. Matthews superstore Car…"). Alternatives rejected: Places API (~5 reviews only), third-party scrapers (£20-50/mo).

## 5E. FLIPDISH DATA EXPANSION (DONE 06-06 night) — file flipdish_expansion_v1_20260606.sql
**KEY DISCOVERY: no new API integration was needed.** The sync already stores per-sale `sale_items` (full item lines incl. nested modifiers), `receipt_lines` (payment lines), `payment_method` (FIRST line only — do NOT use for reconciliation; sales can carry multiple charge lines), `discounts_detail` and `raw_rms` — accumulating since 19 May (this is most of flipdish_sales' 407MB).
- **`store_day_payments`** PK (brand,store,date,payment_method,line_type) from receipt_lines; methods seen: Credit / VenuePaid / Cash / Unknown (98 sales had empty lines). `refresh_store_day_payments(days_back)` REBUILD; **cron job 14** 06:50 (3). **VERIFIED: receipt_net tracks sum(amount_paid) within 0.1-0.5%/day** (gap = null-method sales + rounding) — reconciliation-grade. Cash profile: ~10-12% of takings; Leicester cluster heaviest (London Rd ~£927/day cash), London suburbs near-cashless. **Metro feed query**: cash Charges minus refunds per store/day = expected till banking. OPEN: confirm VenuePaid = platform-settled (Uber Eats/Deliveroo/online remittance) vs Credit = POS card vs Cash = till — determines reconciliation treatment.
- **`item_day_aggregates`** PK (brand,store,date,category,item) from TOP-LEVEL sale_items (nested £0 modifiers excluded in v1); qty, revenue (netRetailPrice×qty, refunded excluded), refunded_qty, comped_qty. `refresh_item_day_aggregates(days_back)` REBUILD; **cron job 15** 06:52 (3). This is the menu-engineering base AND the data foundation for the prep/ordering & waste roadmap phase.
- **First menu read (7 days)**: #1 Waffle Bites ~£16.8k/wk ONCE its duplicate category is merged ("Chocoberry Specials" vs "CHOCOBERRY SPECIALS" — POS case mismatch, FIX AT SOURCE); #2 Banana Pudding ~1,439/£11.5k split across "Viral Banana Pudding" + "Viral Desserts" (split was hiding it); Kanafeh Strawberry Cup £11.7k single item; Falooda ~£11k as a category. v1.1 nicety parked: lower(trim()) category normalisation in the aggregation (POS fix preferred).
- Banking reconciliation + payment mix + top items reports SHIPPED (see §6).
- **PAYMENTS v2 — CHANNEL DIMENSION (06-06 ~23:00, file payments_v2_channel_20260607.sql)**: store_day_payments REBUILT with `channel` in the PK (derived table → DROP+recreate safe; cron job 14 unchanged). **VenuePaid CONFIRMED BY DATA** (channel cross-tab): VenuePaid = platform-settled (UberEats £79.5k / Deliveroo £44.6k / JustEats £8.2k / Flipdish web+kiosk ~£5k per 7 days) → arrives in Metro NET OF COMMISSION (never reconciles 1:1 against our GROSS figures — match against platform statements); POS/Credit £140k = card acquirer settlement (near-1:1); POS/Cash £37.7k = till deposits (the Cash banking report). Payment mix report rebuilt as **settlement lines** (channel × method + "Reaches bank as" column). Item 3d CLOSED.

## 5F. COGS DAY TWO (Sun 07-06 afternoon/evening) — queue item ④ now IN EXECUTION
- **THREE-TIER CORRECTION (Atif)**: batch recipes = CENTRAL KITCHEN bulk production transferred to shops (not shop recipes). Model: suppliers → central kitchen (preps) → shops (assembly). Central kitchen = first-class location; transfers ≠ purchases; variance will split kitchen vs shop.
- **TWO COSTING WORKBOOKS uploaded** (Costing__version_1_.xlsx 23 sheets + Chocoberry_food_costing__1_.xlsx 3): **220 fully costed shop product recipes** parsed (218/224 totals verify vs line sums; Sticky Toffee Pudding + Falafel mismatch). Staged CSVs in container /home/claude/cogs/: ingredients, suppliers, batch_recipes(+lines), shop_recipes(+lines), top60_gp. RE-UPLOAD SOURCE FILES AT BUILD TIME (container resets).
- **TOP-60 GP ANALYSIS (TOP60_GP_ANALYSIS_20260607.xlsx)**: 49/60 costed = £514k of £702k 28d revenue; weighted GP ≈74.4%. Flags: Karak Chai "−67%" = per-BATCH(12 cups) cost vs per-cup ASP (true ~86%); Waffle Bites £0.29 cost suspicious → EXPLAINED by modifier discovery below; "Choco Puff"/"Chocopuffs" POS dup matched different blocks (49.6% vs 92%).
- **CHANNEL ECONOMICS (Atif's catch: blended ASP hides channel truth)**: item_day_aggregates REBUILT v2 with **channel in PK** (items_v2_channel_20260607.sql, RUN) + **channel_commissions table: UberEats 25 / Deliveroo 30 / JustEats 34 / Flipdish web+kiosk 4.5 / POS 0** (contract rates per Atif 07-06). Findings (CHANNEL_ECONOMICS_20260607.xlsx, top-12 items): platforms netted **£24.6k LESS than store-equivalent over 28d**; ONLY Strawberry Mojito nets above POS on UE/Deliveroo (+58-65% premium covers commission — the template); **JustEats structurally broken** (34% commission, stickers often BELOW store price, e.g. Kulfi Falooda £7.11 vs £10.86 POS); FlipdishWebApp ASPs 25-45% below POS (confirm deliberate); Crunch Cake KIOSK £4.96 vs £10.93 = likely misconfig. Repricing targets per item/channel in col J.
- **MODIFIER DISCOVERY (Atif's catch #2)**: modifiers = **£140,332 = 10.4% of 28d revenue**, 121,813 lines (47,522 paid). TOP "MODIFIERS" ARE VARIANT SELECTORS: Full £55.3k / Half / Large / 6pcs / Chicken/Beef/Veg / Full-Round — POS models size+option variants as modifiers → EXPLAINS Waffle Bites' £0.29/£8.19 anomaly (real price+size lives in the modifier). Also bundle_choice modifiers (Spanish Latte (Hot), Milk Hot Chocolate) = MEAL DEALS exist. Whipped Cream partially paid (~61p avg). **Architecture v1.1: modifier kinds = variant (replacement recipe per product+variant pair) | addon (standalone mini-recipe) | bundle_choice (points at existing product)**. item_day_aggregates v3 (future) must carry modifier rows with parent linkage for true ticket ASP.
- **ARCHITECTURE SIGNED OFF (COGS_ARCHITECTURE_v1_20260607.md)**: base units g/ml/ea with import conversion ✓; all prices ex-VAT ✓; process loss 0 (variance reveals) ✓; weekly counts shops+central kitchen ✓; templates written for OPS PERSON ✓; packaging single-per-product v1 (channel override later, unanswered→recommendation). Principles: versioned prices/recipes (effective_from, history never rewritten); pos_aliases (POS names ≠ identities); single stock_movements ledger; imports validate-or-reject to named-row review queue.
- **DATA PACK ISSUED: COGS_DATA_PACK_v1_1_20260607.xlsx** (supersedes v1) — READ ME (7 golden rules for ops) + A Ingredients (477 prefilled, 159 prices yellow, unit dropdowns) + B Preps (49 recipes/316 lines, **34 yields yellow = pack priority**) + C Products (1,207 lines from costing workbooks) + D Modifiers (top-20 prefilled+classified, full list at import) + E POS aliases (top-60 + suggestions). Ops fills → returns → import validates row-by-row.
- **NEXT BUILD (Claude)**: schema SQL per signed-off architecture (tier 0-3 + engine: product_day_costs, gp_day, theoretical_usage_day) + import script consuming this exact pack + modifier-aware item aggregation v3.

## 6. WHAT'S DEPLOYED IN THE APP (all live @ commit 8279784)
- **Banking + menu reports (06-06 night)**: ReportsView now **14 reports**. **Cash banking** (cash Charges minus refunds per store/day from store_day_payments, newest-first = the Metro reconciliation feed; ACCEPTANCE TEST PENDING: Atif to compare a manually-reconciled week vs this report). **Payment mix** (Credit/VenuePaid/Cash net of refunds + share%). **Top items** (top 100 by revenue: qty, avg price, share, refund/comp flags; categories NORMALISED in-report — case/whitespace — so the POS duplicates merge before the POS fix). supabase.js: fetchStoreDayPayments, fetchItemDayAggregates.
- **Onboarding Board (06-06 final)**: nav "onboarding-board" (PEOPLE group, owner/hq ONLY), read-only compliance grid, one row per active ops_team member, SORTED most-outstanding-first. Columns: Profile (profile_status) · RTW (employee_documents matched via doc_type/required_doc_key ilike %rtw%; ladder Submitted→HR review→Verified/Rejected; hr_approved_at = done) · Contract (employee_contracts excl voided; signed→Signed, any live→Sent) · Training (completed required training_modules for the member's store, x/y) · Policies (policy_acknowledgements COUNT — NO required-policy set exists in schema yet, parked) · Starter tax (ops_team.tax_completed_at) · Bank (bank_provided_at) · Trainee/Staff (is_trainee). Filters: store + scope (Onboarding only = is_trainee OR profile_status≠complete | Everyone). Chips: done/progress/missing/rejected. supabase.js: fetchContractStatuses, fetchRtwDocuments, fetchTrainingOverview, fetchPolicyAcks (compact all-employee fetches). Board is READ-ONLY; actions happen in Team/Hiring/Training/Contracts.
- **Sales reports + date guard (06-06 late)**: 4 new ReportsView cards (11 total) — **Sales by store** (days/orders/gross/net/ATV/discounts/cancelled/refunded per store; TOTAL net for 19 May→ range should reconcile to known figures, ~£769k at build time), **Channel mix** (orders/revenue/ATV/share% per channel; parser tolerates by_channel as {ch:{orders,revenue}} OR {ch:number}), **Day-of-week performance** (Mon-first averages), **Forecast vs actual** (scored 1-day-ahead rows + avg abs%; emptyHint until first scored day). New supabase.js: fetchStoreDayAggregates, fetchForecastAccuracyRows(horizon=1 default). **DATE GUARD**: runReport auto-swaps inverted From>To for ALL reports. Reports subtitle updated; report.emptyHint wired into empty state.
- **Accuracy scoreboard + anomaly icon (06-06 eve)**: ForecastPanel accuracy badge now a BUTTON → **ForecastAccuracyModal**: per-horizon MAPE table (mean + median) from forecast_accuracy_summary + v1-vs-v2 comparison from forecast_accuracy_by_method ("if v2 isn't beating v1 after a few weeks, tune the priors"); self-explaining empty state until scored days exist. KIND_ICON maps (x2) gained anomaly:"⚡". supabase.js: fetchForecastAccuracyByMethod.
- **Forecast UI (06-06)**: **ForecastPanel** (next-7-days bars, accuracy badge "warming up"→"typically ±x% one day out" at ≥14 scored days, "thin" markers) mounted in TWO places: bottom of StoreAnalytics (managers, storeId-scoped, stores={[store]}) + bottom of ChainPerformanceView (owner/HQ, storeId=null chain totals, stores prop). Days CLICKABLE → **ForecastDayModal**: Level 1 per-store breakdown for the date (sorted, thin flags; managers jump straight to Level 2) → Level 2 per store: "WHY THIS NUMBER" decomposition from factors jsonb (base × event × weather; v1 rows fall back gracefully) + hourly curve from fetchStoreHourForecasts. supabase.js: fetchStoreDayForecasts (lowest-horizon-wins dedupe, includes factors), fetchForecastAccuracySummary, fetchStoreHourForecasts (freshest horizon's curve).
**Session 06-05/06:**
- **Notification system**: `notifications` table (recipient_type 'user'|'ops', recipient_id, kind, title, body, link_view, read_at; notifications.sql RUN). supabase.js: fetch/markRead/markAll/insert + **notifyManagers({brandId,storeId,kind,title,body,linkView,excludeUserId})** (owners/hq always, managers on brand+store match; fire-and-forget) + **notifyOpsMember(id,{...})**. **NotificationBell** (self-contained 60s poll; UNREAD-ONLY list; dismiss-on-click; Web-Audio chime only when count rises on background poll; onNavigate(linkView); "View all"); placed in Sidebar (panel opens upward, navigates via setActiveView), EmployeeShell header (navigates ITS internal setActiveView), TraineePortal (dismiss-only). **NotificationsView** page (nav "notifications"): latest 100 read+unread, All/Unread filter, click = read + navigate. KIND icons: application📋 task✅ training🎓 staff👤 sync⚠️("Data health"). **4 event hooks**: addApplication→managers(linkView "hiring"); addAssignment employee+personId→that member(linkView "ops-tasks"); TraineePortal module done→managers("training"); convertToStaff→managers("team"). ⚠️ linkViews MUST match real NAV_GROUPS keys (first guesses were wrong).
- **Clear-on-view badges** (Team + Hiring only): these are WORK counters (Team=pending_setup members; Hiring=applied/manager_reviewing apps). Sidebar: `badgeClearOnView:true` items hide after visit, REAPPEAR when count rises past seen; seen clamps down when count drops; per-user localStorage `cb_badge_seen_<userId>`.
- **Member delete fix**: deleteOpsTeam now try/catches with FK-aware error toast (was silently failing). FK playbook: Postgres names only the FIRST violating table; delete child rows by employee_id, retry, repeat. Archive is correct for real ex-staff.
- **Reports section** (nav "reports", roles owner/hq_staff/manager): 6 timesheet reports — store-summary, employee-detail, daily attendance (LATE = >5min after scheduledStart), scheduled-vs-actual variance (fetchSchedulesRange NEW; overnight wraps +24), overtime (Approved/Pending/Rejected), exceptions (missing punch-out / amendedBy / !approved). ONE {title,columns,rows,totals} structure drives BOTH on-screen table and styled ExcelJS export. Managers store-scoped. ⚠️ punches with NULL store_id invisible to store-filtered runs.
- **Sync-now button**: runFlipdishSync invokes `flipdish-rms-sync` with x-sync-secret from `REACT_APP_SYNC_SECRET` (Vercel env, baked at BUILD time). Old dead "flipdish-sync" reference removed.
- **Labour vs revenue report (06-06)**: 7th card in ReportsView — reads `labour_vs_revenue` view via NEW supabase.js `fetchLabourVsRevenue({from,to})` (not punches); per store/day: net revenue, hours, OT hrs, labour cost, labour %, rev/hour; TOTAL row with blended %; 'unmatched' shown as "⚠ Unmatched"; '—' where one join side missing; managers store-scoped; Excel export free via unified structure. Errors "relation does not exist" if labour SQL statements 1/2/5 not run.
**Earlier sessions**: Store Analytics dashboard + v2 (gross/net toggle, discount rate, channel ATV, prev-period overlay, day-parts, heatmap toggle) for managers; hiring→"Onboarding" relabel; new-hire trainee defaults; profile pending/complete (17 required fields); employee email+PIN login; stepped training (`---` splits module into cards); outage root-cause & fixes.
**Commit chain**: … f592d54 (v11) → a19df5c (settlement PARTIAL) → 944c22b (settlement repaired) → c9c5741 (handoff v12) → **[ask-data commit — hash not captured, user confirmed deployed + view live] ← LIVE**.
**Canonical files**: App_ASKDATA.js **1,307,184 bytes** + supabase_ASKDATA.js **197,738 bytes** = deployed code.
**SECOND MISSED-COPY INCIDENT (ask-data deploy)**: supabase copy skipped again; the UNIQUE marker ("export async function askData") correctly BLOCKED the commit this time (no false-pass) — the sharpened-marker gotcha from a19df5c proven within the hour.
**DEPLOY INCIDENT (a19df5c)**: supabase_SETTLEMENT.js was never downloaded; `copy` said "cannot find the file" but the chain proceeded because the findstr marker (fetchStoreDayPayments) also existed in the OLD file → new report shipped against old fetch. Caught by byte check one message later, repaired in 944c22b. **GOTCHA EARNED: findstr markers must be unique to the NEW code; any "cannot find the file" or missing file in dir output = STOP the chain.** SQL run: notifications.sql, sync_health_check.sql, store_day_aggregates.sql (fixed), labour_day_aggregates_v1 (all 8), phase1_forecast_engine_v1 (all 8), phase1_hourly_v1 (all 4), forecast_v2 (all 9 + school-holiday & Walsall-closure calendar inserts), phase2a_anomaly_alerts (all 5 + manager-extension replace).

## 7. SECURITY & SESSION LOG (06-06)
- ✅ **`super-api` Edge Function DELETED** — appeared unexplained ~04:30 on 06-06; 0 invocations; deleted.
- ✅ **`flipdish-rms-sync_index-ts` DELETED** — accidental duplicate from file-upload deploy; never called.
- ✅ **SHARED SECRET ROTATED (06-06 PM)** — old `chocoberryflipdishsync2026` dead everywhere. New value lives in **5 places** (exact-match): ① function env `SYNC_SHARED_SECRET` ② cron job 5 x-sync-secret header ③ RPC `set_flipdish_tokens` hardcoded `expected :=` (SECURITY DEFINER; recreate via CREATE OR REPLACE to change) ④ extension `shared.js` CONFIG.SHARED_SECRET ⑤ Vercel `REACT_APP_SYNC_SECRET` (+redeploy). All five verified live (manual http_post 200, extension popup ✓, app Sync Now ✓).
- ⚠️ **INVISIBLE-CHARACTER GOTCHA (cost ~1h)**: pasting the secret poisoned one copy with an invisible char — SQL dummy-test "passed" because the test was pasted from the same dirty clipboard, while the extension's clean string 401'd. Diagnosis path that worked: service-worker console → `CONFIG.SHARED_SECRET.length` + charcode dump (`[...s].map(c=>c.charCodeAt(0))`), then recreate the RPC from a clean-typed value. RULE: when secrets mismatch despite "looking identical", check charcodes; propagate from ONE clean source.
- ⚠️ Extension's popup "Auto-refresh last ran ✗" line is HISTORY of the previous background run, not current state.
- DECIDED: current secret appeared in the working chat — Atif accepts the (modest) risk; no further secret rotation planned. Service-role key rotation OFFERED but not yet done (2-min job, nothing audited uses old key: extension=anon key, cron=secret header, function=injected env).
- AUDIT FACT: extension uses ANON key + RPC secret; safe against service-role rotation.
- ✅ **WEBHOOK-INTAKE LEAK FOUND & KILLED (06-06 late)**: the "dead since 19 May" webhook pipeline's INTAKE never died — Flipdish webhook subscription (their portal: OAuth app → Webhooks, feature now Deprecated) was still POSTing every order to Edge Function **`flipdish-webhook`** (JWT off, undocumented survivor), which wrote full body+headers jsonb to `flipdish_webhook_log`: 44,482 rows / **103 MB**, growing ~100MB per 3 wks, and was the main cause of the free-tier 115% db breach. KILL = both ends: Edge Function deleted + Flipdish subscription deleted; flipdish_webhook_log + flipdish_orders TRUNCATED (kept as empty tables — supabase.js still has dead fetch fns referencing them). ~107MB reclaimed (db 575→~468MB). flipdish_sales (407MB) is the legitimate live asset.
- ⏳ **Service-role key rotation — REVISED SCOPE**: Supabase moved to new key system (publishable/secret); the exposed legacy service_role JWT shares a signing secret with the legacy ANON key, so rotating invalidates BOTH → must update anon in supabase.js + extension shared.js + redeploy + reload (~15-min coordinated job, NOT a 2-min button). Still open; good next-session opener.

## 8. 🎯 AI & DATA-DRIVEN OPS ROADMAP (agreed; benchmark Nory.ai)
**Principles**: data foundations FIRST · statistical forecasting (NOT LLM) for all numbers · Claude API (server-side Edge Fn — APPROVED by Atif) only for narrative/interpretation · forecast accuracy tracked & displayed honestly.
**Decision sequence**: 1) site performance + anomaly alerts (least history, zero manager behaviour change, builds trust) → 2) staffing/labour (biggest lever, needs history+adoption) → 3) prep/ordering & waste.
**Phases**: 0.1 sync watchdog ✅ · 0.2 aggregates ✅ · 0.3 labour aggregates ✅ (data gated on punch ADOPTION) · 0.4 token hardening (open) · **Phase 1 forecast engine ✅ (06-06, v1+v2 — see §5B)** · **Phase 2A anomaly alerts ✅ (06-06 eve — see §5C)** · **Phase 2B staffing recommendations — DATA-GATED** (needs punch rollout + ~2 wks accuracy data, ~late June) · **Phase 3 slice 1 weekly narrative ✅ LIVE (06-06 close — see §5D)**; slices 2-3 (alert explanations, ask-your-data chat) + narrative viewer UI + calendar-context-in-bundle v2 = next builds.
**Constraints**: ~3 wks clean history; no customer-level data; EOD is brand-level (fine for labour%).

**Phase 0.3 design facts**: `labour_day_aggregates` PK (brand,store,business_date); hours/cost from COMPLETED punches only (punch_out NOT NULL); cost=COALESCE(gross_pay, hours×hourly_rate, 0); approved-only OT; 'unmatched' bucket; REBUILD semantics; view `labour_vs_revenue` FULL OUTER JOIN onto sales aggregates.

## 9. OPEN ITEMS — PRIORITY ORDER
**STATE AS OF v11: the buildable backlog is EMPTY.** Everything below needs either Atif's input/action or data that arrives with time. Next session starts with whichever gate has opened.
1. **Rotate legacy JWT secret** (~15-min coordinated: invalidates legacy anon + service_role together → new anon into supabase.js + extension shared.js + redeploy + extension reload; see §7). Best fresh-session opener.
2. **Punch in/out rollout to stores** — labour data gated on this. Watch first live days via Exceptions report + labour report 'unmatched' rows.
3. **Calendar curation (ongoing, no code)**: confirm Islamic dates nearer time; refine school-holiday dates per council; add local events; tune priors as actuals land; **delete the Walsall closure row if it reopens**.
3b. **Flipdish data expansion ✅ DONE (see §5E)** — payments + items were already in jsonb; no API work was needed. Next: banking reconciliation report + menu reports in ReportsView.
3c. **Narrative v2.1 ✅ DEPLOYED (06-06 ~23:15, user-confirmed)** — Monday 07:40's automated report is the first under the no-invented-calendar-facts rule.
3d. **VenuePaid ✅ CONFIRMED + settlement lines shipped (see §5E)**.
3e. **POS category cleanup**: merge "CHOCOBERRY SPECIALS"/"Chocoberry Specials" + Banana Pudding's two categories at the POS.
4. **BUILD: Phase 2B** staffing recommendations (see §8) — gated on punch rollout + ~2 wks accuracy data (~late June). 2A threshold tuning via forecast_anomaly_check view as verdicts accrue.
5. Backlog: learn weather/event coefficients from actuals (replace seeded priors once months of weather_daily actuals exist); manual-timesheet support in labour refresh IF back-filling becomes real; hourly_rate sourcing for labour cost; sales/performance reports in ReportsView; email layer (Resend); define a required-policy set for the Onboarding Board's Policies column (shows raw ack counts until then); real employee passwords (employeeSignIn via Supabase Auth already exists dormant — finishing = invite/signup flow); kiosk breaks stage 2; login background.

## 10. KNOWN GOTCHAS QUICK LIST
- Downloads "(1)" trap · empty-findstr = STOP · one CMD command per line (pasted blocks execute everything; typing a .js path EXECUTES it — attach files via the chat paperclip, never CMD) · don't paste SQL output into SQL editor · React.use* forbidden · net.http_post async (read net._http_response) · sync upsert key (sale_id,brand_id) duplicates on brand reclassification · derived tables REBUILD don't patch · rolling window OVERWRITES manual store_id patches for yesterday/today · unread sync alerts muffle future ones · CRA env vars baked at build · Edge Function edits go in-place (file upload creates new function; NEW functions via dashboard editor are fine — weather-sync was made this way) · manual watchdog tests before 06:30 UTC false-positive · pasted secrets can carry INVISIBLE characters — verify via charcode dump, propagate from one clean source · Reports date inputs: From>To returns zero rows silently · labour aggregates ignore hours-without-punch_out rows by design · closed stores keep forecasting from stale history unless a closure calendar row (uplift 0) exists · retroactive calendar rows are how you CLEAN bases, not just predict future · forecast horizon rows accumulate by design (accuracy per lead time) — display code takes lowest horizon per date · DEAD PIPELINES: consumers dying ≠ intake dying — audit Flipdish portal webhook registrations AND the Edge Function list for surviving receivers · legacy anon + service_role share one JWT signing secret: rotating one kills both.
<!-- HANDOFF_V14 -->
